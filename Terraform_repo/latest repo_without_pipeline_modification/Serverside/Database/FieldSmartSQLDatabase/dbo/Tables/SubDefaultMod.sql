CREATE TABLE [dbo].[SubDefaultMod] (
    [ScriptID]       INT NOT NULL,
    [SequenceNumber] INT NOT NULL,
    CONSTRAINT [PK_SUBDEFAULTMOD] PRIMARY KEY CLUSTERED ([ScriptID] ASC, [SequenceNumber] ASC)
);

